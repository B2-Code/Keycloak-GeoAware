{
  "dev": {
    "baseUrl": "http://localhost:8080",
    "Security": {
      "Auth": {
        "local-kc": {
          "Token URL": "{{baseUrl}}/realms/master/protocol/openid-connect/token",
          "Auth URL": "{{baseUrl}}/realms/master/protocol/openid-connect/auth",
          "Client ID": "admin-cli",
          "Username": "admin",
          "Password": "admin",
          "Grant Type": "Password",
          "Type": "OAuth2"
        }
      }
    }
  }
}
